name: Deploy Sample App

on:
  push:
    branches:
    - main
  pull_request:

defaults:
  run:
    working-directory: ./app

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make

